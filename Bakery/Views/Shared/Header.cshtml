<div id="header">
  <h1><a href='/'>Pierre's Sweet & Savory Treats</a></h1>

@if (User.Identity.IsAuthenticated)
{
  <h4> @Html.ActionLink("Home", "Index", "Home") | @Html.ActionLink("List of Treats", "Index", "Treats") | 
  @Html.ActionLink("List of Flavors", "Index", "Flavors")</h4>

  <span class="logoutbtn">@using (Html.BeginForm("LogOff", "Account"))
  {
    <input type="submit" class="btn btn-default" value="Log out" />
  }
  </span>
}
  
</div>